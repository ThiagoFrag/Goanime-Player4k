# ═══════════════════════════════════════════════════════════════════════════════
#  🎬 GoAnime Player 4K - Configuração MPV
#  Upscaling de Alta Qualidade + Interpolação 60fps + uosc
# ═══════════════════════════════════════════════════════════════════════════════

# === GPU E PERFORMANCE (PRIMEIRO!) ===
# Usar GPU-HQ como base
profile=gpu-hq
vo=gpu-next
gpu-api=d3d11
hwdec=auto-copy

# === UI (uosc) ===
osc=no
load-scripts=yes
sub-fonts-dir=~~/fonts

# === ESTATÍSTICAS (Pressione I para ver) ===
# Mostra informações de upscaling, fps, etc
script-opts=stats-font_size=8

# ═══════════════════════════════════════════════════════════════════════════════
#  UPSCALING - QUALIDADE MÁXIMA PARA ANIME (SPLINE36 é mais visível)
# ═══════════════════════════════════════════════════════════════════════════════

# Algoritmo de escala - SPLINE36 é excelente e bem visível
# Alternativas: ewa_lanczossharp, ewa_lanczos, lanczos, spline36
scale=ewa_lanczos
scale-blur=0.981251
cscale=ewa_lanczos
dscale=mitchell

# Antiringing (reduz halos/artefatos)
scale-antiring=0.7
cscale-antiring=0.7
dscale-antiring=0.7

# Sigmoid upscaling (melhora transições de cor)
sigmoid-upscaling=yes
sigmoid-center=0.75
sigmoid-slope=6.5
linear-downscaling=no
correct-downscaling=yes

# ═══════════════════════════════════════════════════════════════════════════════
#  INTERPOLAÇÃO 60FPS (Motion Smoothing para Anime)
# ═══════════════════════════════════════════════════════════════════════════════

# Sincroniza vídeo com taxa do monitor
video-sync=display-resample

# Habilita interpolação de frames
interpolation=yes

# Algoritmo de interpolação
tscale=oversample
tscale-window=sphinx
tscale-radius=1.0
tscale-clamp=0.0

# ═══════════════════════════════════════════════════════════════════════════════
#  DEBAND (Remove banding comum em anime)
# ═══════════════════════════════════════════════════════════════════════════════

deband=yes
deband-iterations=4
deband-threshold=35
deband-range=16
deband-grain=5

# ═══════════════════════════════════════════════════════════════════════════════
#  DITHER (Melhor gradiente de cores)
# ═══════════════════════════════════════════════════════════════════════════════

dither-depth=auto
dither=fruit
temporal-dither=yes

# ═══════════════════════════════════════════════════════════════════════════════
#  OSD (MENSAGENS NA TELA) - TEMA GOANIME
# ═══════════════════════════════════════════════════════════════════════════════

osd-level=1
osd-duration=1500
osd-font='Segoe UI'
osd-font-size=26
osd-bold=yes
osd-color='#FFFFFF'
osd-border-color='#FF6B9D'
osd-border-size=2
osd-shadow-offset=1
osd-shadow-color='#CC0D0D1A'
osd-back-color='#99000000'
osd-bar=no
osd-bar-h=2
osd-bar-w=80
osd-bar-align-y=0.95
osd-status-msg=
osd-playing-msg=

# ═══════════════════════════════════════════════════════════════════════════════
#  LEGENDAS
# ═══════════════════════════════════════════════════════════════════════════════

sub-auto=fuzzy
sub-file-paths=subs:subtitles:Subs:Subtitles:legendas
sub-font='Segoe UI Semibold'
sub-font-size=44
sub-color='#FFFFFF'
sub-border-color='#000000'
sub-border-size=2.5
sub-shadow-offset=1
sub-shadow-color='#80000000'
sub-margin-y=50
sub-blur=0.2

# ═══════════════════════════════════════════════════════════════════════════════
#  ÁUDIO
# ═══════════════════════════════════════════════════════════════════════════════

audio-pitch-correction=yes
volume=100
volume-max=150

# ═══════════════════════════════════════════════════════════════════════════════
#  CACHE (STREAMING)
# ═══════════════════════════════════════════════════════════════════════════════

cache=yes
demuxer-max-bytes=150MiB
demuxer-max-back-bytes=75MiB
demuxer-readahead-secs=60

# ═══════════════════════════════════════════════════════════════════════════════
#  JANELA
# ═══════════════════════════════════════════════════════════════════════════════

keep-open=yes
border=no
autofit-larger=90%x90%
cursor-autohide=1000
force-window=immediate
window-maximized=yes
background='#000000'
title=🎬 ${media-title} - GoAnime Player

# Não pular frames
framedrop=no
video-latency-hacks=no

# ═══════════════════════════════════════════════════════════════════════════════
#  SCREENSHOTS
# ═══════════════════════════════════════════════════════════════════════════════

screenshot-format=png
screenshot-png-compression=7
screenshot-template=GoAnime_%F_%P
screenshot-directory=~~desktop/

# ═══════════════════════════════════════════════════════════════════════════════
#  INPUT
# ═══════════════════════════════════════════════════════════════════════════════

input-default-bindings=yes
input-vo-keyboard=yes
