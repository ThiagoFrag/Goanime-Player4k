# ═══════════════════════════════════════════════════════════════════════════════
#  🎬 GoAnime Player - Atalhos de Teclado
#  Intuitivos e fáceis de usar
# ═══════════════════════════════════════════════════════════════════════════════

# === INFORMAÇÕES (I) ===
i script-binding stats/display-stats
I script-binding stats/display-stats-toggle
TAB script-binding uosc/toggle-ui

# === PLAY/PAUSE ===
SPACE cycle pause
MBTN_LEFT_DBL cycle fullscreen
MBTN_RIGHT cycle pause

# === NAVEGAÇÃO INTELIGENTE ===
RIGHT seek 5 exact
LEFT seek -5 exact
UP seek 60 exact
DOWN seek -60 exact
Shift+RIGHT seek 87 exact; show-text "⏭️ Skip +90s"
Shift+LEFT seek -87 exact; show-text "⏮️ Skip -90s"
l seek 85 exact; show-text "⏭️ Skip Intro (~90s)"
. frame-step
, frame-back-step
HOME seek 0 absolute
END seek 100 absolute-percent

# === VOLUME ===
WHEEL_UP add volume 5; show-text "🔊 Volume: ${volume}%"
WHEEL_DOWN add volume -5; show-text "🔊 Volume: ${volume}%"
m cycle mute; show-text "🔇 Mudo: ${mute}"
9 add volume -5
0 add volume 5

# === VELOCIDADE (fácil de lembrar) ===
[ multiply speed 0.9; show-text "⏪ ${speed}x"
] multiply speed 1.1; show-text "⏩ ${speed}x"
{ multiply speed 0.5; show-text "⏪ ${speed}x"
} multiply speed 2.0; show-text "⏩ ${speed}x"
BS set speed 1.0; show-text "▶️ Velocidade normal"

# === TELA ===
f cycle fullscreen
ENTER cycle fullscreen
ESC set fullscreen no
F11 cycle fullscreen

# === LEGENDAS ===
v cycle sub-visibility; show-text "📝 Legendas: ${sub-visibility}"
j cycle sub; show-text "📝 Legenda: ${current-tracks/sub/title}"
J cycle sub down
z add sub-delay -0.1; show-text "📝 Delay: ${sub-delay}s"
Z add sub-delay 0.1; show-text "📝 Delay: ${sub-delay}s"
x add sub-delay 0; show-text "📝 Delay resetado"

# === ÁUDIO ===
a cycle audio; show-text "🔊 Áudio: ${current-tracks/audio/title}"
A cycle audio down
+ add audio-delay 0.100; show-text "🔊 Audio delay: ${audio-delay}s"
- add audio-delay -0.100; show-text "🔊 Audio delay: ${audio-delay}s"

# === QUALIDADE/FILTROS ===
d cycle deband; show-text "✨ Deband: ${deband}"
D cycle-values deband-iterations 1 2 4 8; show-text "✨ Deband força: ${deband-iterations}"
u cycle interpolation; show-text "🎞️ Interpolação 60fps: ${interpolation}"
h cycle-values hwdec "auto-copy" "no"; show-text "🖥️ Hardware decode: ${hwdec-current}"

# === SCREENSHOT ===
s screenshot; show-text "📸 Screenshot salvo!"
S screenshot video; show-text "📸 Screenshot (sem legendas)"

# === ASPECT RATIO ===
Ctrl+a cycle-values video-aspect-override "16:9" "4:3" "2.35:1" "-1"; show-text "📐 Aspect: ${video-aspect-override}"

# === ZOOM/PAN ===
Alt+= add video-zoom 0.1; show-text "🔍 Zoom: ${video-zoom}"
Alt+- add video-zoom -0.1; show-text "🔍 Zoom: ${video-zoom}"
Alt+UP add video-pan-y 0.05
Alt+DOWN add video-pan-y -0.05
Alt+LEFT add video-pan-x 0.05
Alt+RIGHT add video-pan-x -0.05
Alt+BS set video-pan-x 0; set video-pan-y 0; set video-zoom 0; show-text "🔍 Zoom/Pan resetado"

# === PLAYLIST ===
> playlist-next; show-text "⏭️ Próximo"
< playlist-prev; show-text "⏮️ Anterior"
PGUP playlist-prev
PGDWN playlist-next

# === LOOP ===
L cycle-values loop-file "inf" "no"; show-text "🔁 Loop: ${loop-file}"

# === MENU uosc ===
MBTN_MID script-binding uosc/menu
Menu script-binding uosc/menu

# === QUIT ===
q quit
Q quit-watch-later

# === DICAS RÁPIDAS ===
# F1 = Ajuda
F1 show-text "🎬 GoAnime Player\\n\\nI = Estatísticas\\nTAB = UI\\nL = Skip intro\\nd = Deband\\nu = 60fps\\n[ ] = Velocidade\\nf = Fullscreen\\ns = Screenshot" 5000
